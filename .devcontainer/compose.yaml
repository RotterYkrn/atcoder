services:
    atcoder:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: atcoder
        stdin_open: true
        tty: true
        volumes:
            - ..:/workspace:cached
            - acc-session:/root/.config/atcoder-cli-nodejs/session.json
            - oj-config:/root/.local/share/online-judge-tools

volumes:
  acc-session:
  oj-config: